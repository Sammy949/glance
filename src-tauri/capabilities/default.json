{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window. App-defined commands (get_launch_file) and events (open-file) need no extra permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
